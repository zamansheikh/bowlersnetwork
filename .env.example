# API Configuration
NEXT_PUBLIC_API_URL=https://your-api.com/api
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Server Port (used by next dev / next start)
# Copy this file to .env.local and update as needed.
PORT=5444

# Mapbox API Configuration
# Get your public token from: https://account.mapbox.com/access-tokens/
# This is a PUBLIC token and is safe to expose in client-side code
# Make sure to set URL restrictions in your Mapbox dashboard
NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN=your_mapbox_public_token_here

# Authentication
NEXTAUTH_SECRET=your-secret-key
NEXTAUTH_URL=http://localhost:3000
